# 🏋️‍♂️ Gym Tracker

**Gym Tracker** — это современное веб-приложение для учета тренировок, анализа прогресса и построения персональных программ. Простой, красивый и функциональный трекер для спортсменов и любителей фитнеса.

---

## 🚀 Особенности

- **Трехдневная программа тренировок**  
  Дни A, B и C с подборкой упражнений для всех групп мышц.

- **Запись подходов и веса**  
  Легко добавлять новые подходы с весом и повторениями, плюс кнопка «+1 подход» для ускоренного ввода.

- **История тренировок**  
  Полный журнал всех подходов с возможностью удаления старых записей.

- **Графики прогресса**  
  Визуализация веса и повторов для каждого упражнения с помощью **Chart.js** и плавными градиентами.

- **Персональные рекомендации**  
  Подробная программа с оптимальными подходами и весами для каждого дня.

- **Современный интерфейс**  
  Минималистичный дизайн на **Bootstrap 5**, тёмная тема, анимации карточек и плавные hover-эффекты.

---

## 🛠️ Технологии

- **Python 3.10+**  
- **Flask** — веб-фреймворк для серверной части  
- **SQLite** — хранение логов тренировок  
- **Bootstrap 5 & Bootstrap Icons** — UI и иконки  
- **Chart.js** — интерактивные графики прогресса

---

## 🏗️ Структура проекта

```text
gym_tracker/
├─ app.py              # Главный Flask-файл
├─ database.db         # SQLite база данных
├─ requirements.txt    # Зависимости
├─ templates/
│  ├─ base.html        # Базовый шаблон
│  ├─ index.html       # Главная страница
│  └─ workout.html     # Страница тренировки
└─ static/
   └─ style.css        # Стили (по желанию)
````

---

## ⚡ Быстрое создание проекта

Чтобы не копировать файлы вручную, можно воспользоваться этим скриптом на Python, который создаст актуальную структуру **Gym Tracker**:

```python
import os

PROJECT_NAME = "gym_tracker"

# Актуальная структура проекта
structure = {
    "app.py": "",
    "database.db": "",
    "requirements.txt": "flask\nmatplotlib\n",
    "templates": {
        "base.html": "",
        "index.html": "",
        "workout.html": ""
    },
    "static": {
        "style.css": ""
    }
}

def create_structure(base_path, tree):
    for name, content in tree.items():
        path = os.path.join(base_path, name)
        if isinstance(content, dict):
            os.makedirs(path, exist_ok=True)
            create_structure(path, content)
        else:
            with open(path, "w", encoding="utf-8") as f:
                f.write(content)

def main():
    base_path = os.path.join(os.getcwd(), PROJECT_NAME)
    os.makedirs(base_path, exist_ok=True)
    create_structure(base_path, structure)
    print(f"Проект '{PROJECT_NAME}' успешно создан с актуальной структурой!")

if __name__ == "__main__":
    main()
```

✅ После запуска скрипта появится структура:

```text
gym_tracker/
├─ app.py
├─ database.db
├─ requirements.txt
├─ templates/
│  ├─ base.html
│  ├─ index.html
│  └─ workout.html
└─ static/
   └─ style.css
```

С этим скриптом можно быстро подготовить чистый проект и сразу начинать разрабатывать **Gym Tracker**. ⚡

---

## 🏗️ Установка и запуск

1. Клонируем репозиторий (или используем скрипт выше):

```bash
git clone https://github.com/<your-username>/gym_tracker.git
cd gym_tracker
```

2. Создаем и активируем виртуальное окружение:

```bash
python -m venv venv
# Windows
venv\Scripts\activate
# Linux / macOS
source venv/bin/activate
```

3. Устанавливаем зависимости:

```bash
pip install -r requirements.txt
```

4. Запускаем приложение:

```bash
python app.py
```

5. Открываем браузер и переходим по адресу:

```
http://127.0.0.1:5000
```

---

## 🎯 Как использовать

1. **Главная страница** — выбор дня тренировки или переход к графикам и истории.
2. **Страница тренировки** — выбираем упражнение, вводим вес и количество повторов, сохраняем подход.
3. **История тренировок** — просматриваем все прошлые подходы, при необходимости удаляем.
4. **Графики** — анализируем прогресс по каждому упражнению.
5. **Рекомендации** — следуем персональной программе тренировок.

---

## 💡 Идеи для развития

* Аутентификация пользователей
* Экспорт данных в CSV / Excel
* Интеграция с фитнес-трекерами
* Умные рекомендации на основе прогресса

---

## ⚖️ Лицензия

MIT License — свободно использовать, модифицировать и распространять.
